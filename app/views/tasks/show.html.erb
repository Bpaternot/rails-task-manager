<h1>Task's details</h1>

<h2><%= @task.name %></h2>
<p><%= @task.description %></p>
<p><%= @task.done %></p>
<%= link_to "Back to list", tasks_path, method: :get%>
<%= link_to "Delete Task", task_path(@task), method: :delete%>
